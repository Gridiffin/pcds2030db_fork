# Environment configuration (rename to .env and modify as needed)
NODE_ENV=development

# Server settings
PORT=3000
HOST=0.0.0.0

# Optional: Set custom storage path (used in production)
# STORAGE_PATH=/path/to/storage
/**
 * Admin Outcomes JavaScript Entry Point
 * Imports modular CSS and initializes outcomes functionality
 */

// Import outcomes-specific CSS bundle
import '../../css/admin/outcomes/outcomes.css';

// Outcomes functionality is handled by manage_outcomes.js loaded separately

// Outcomes initialization
document.addEventListener('DOMContentLoaded', function() {
    console.log('Admin Outcomes loaded');
    
    // Initialize outcomes-specific features
    initializeOutcomes();
});

function initializeOutcomes() {
    // Outcomes-specific initialization code
    console.log('Outcomes module initialized');
}
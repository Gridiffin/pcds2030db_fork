/**
 * Admin Reports JavaScript Entry Point
 * Imports modular CSS and initializes reports functionality
 */

// Import reports-specific CSS bundle
import '../../css/admin/reports/reports.css';

// Reports functionality is handled by reports.js loaded separately

// Reports initialization
document.addEventListener('DOMContentLoaded', function() {
    console.log('Admin Reports loaded');
    
    // Initialize reports-specific features
    initializeReports();
});

function initializeReports() {
    // Reports-specific initialization code
    console.log('Reports module initialized');
}